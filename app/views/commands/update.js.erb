$("#defaultModal").modal("hide")
<% if @command.errors.empty? %>
  notify('指令更新成功');
  datatableUpdate(removeTags('<%= j render('show') %>'), $("[data-href='<%= @data_href %>']").parent().parent())
<% else %>
  var text = '指令更新失败: ' + '<%= @command.first_error_message %>'
  notify(text);
<% end %>