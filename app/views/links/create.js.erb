$("#defaultModal").modal("hide");
<% if @link.errors.empty? %>
  notify('指令创建成功');
  datatableCreate(removeTags('<%= j render('show') %>'))
<% else %>
  var text = '指令创建失败: ' + '<%= @link.first_error_message %>'
  notify(text);
<% end %>